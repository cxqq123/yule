// 统一管理依赖的库
ext {

    // android 编译相关
    android = [

    ]

    // Android SDK/Tools Version
    compileSdkVersion = 31
    buildToolsVersion = '30.0.2'
    minSdkVersion = 24
    targetSdkVersion = 31
}